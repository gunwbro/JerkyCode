extends layout

block content
    .techsPost-container
        .techsPost
            for tech in techs
                .techs-container
                    a.techs-title(href='/tech/' + tech.id)= tech.title
        div
            if (admin)
                a.writeProject(href='/tech/post') Post
        