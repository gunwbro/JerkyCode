extends layout

block content
  .tag-container
    for tag in tags
      - let fontSize = numPost[tag.id] * 20;
      a#tag(href='/tag/' + tag.id, style={ 'font-size': fontSize+'px'})= '#' + tag.title + '(' + numPost[tag.id] + ') '